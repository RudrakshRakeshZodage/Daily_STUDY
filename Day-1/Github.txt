# 1. Create the README file
echo "# Daily_STUDY" >> README.md

# 2. Initialize the git repository
git init

# 3. Add all files to the staging area
git add .

# 4. Commit the changes
git commit -m "first commit"

# 5. Rename the current branch to 'main'
git branch -M main

# 6. Add the remote repository URL
git remote add origin https://github.com/RudrakshRakeshZodage/Daily_STUDY.git

# 7. Push the code to the 'main' branch on GitHub
git push -u origin main






# 1. Add all modified files to the staging area
git add .

# 2. Commit the changes with a message
git commit -m "Update code changes"

# 3. Push the changes to GitHub
git push